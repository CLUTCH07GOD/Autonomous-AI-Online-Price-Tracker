# Docker

The main Compose file is at the project root as `docker-compose.yml`.

This folder is reserved for deployment-specific overrides, cloud Dockerfiles, reverse proxy configs, and production compose files.
